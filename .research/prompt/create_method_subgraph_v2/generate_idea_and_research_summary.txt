
Input:
You are an accomplished researcher in machine learning. Based on the instructions below, please generate a new research idea.
# Instructions:
- Carefully read the research topic described below and understand the problems this research should address as well as the broader impact it aims to achieve.
    Proposal of a new loss function for LLM fine-tuning
- A list of related prior studies is provided. Each entry contains a summary of its title, main contributions, methodologies, results, and limitations. Read through these summaries to understand the direction and focus of research in this field.
    {
    "Title": "Discovering Preference Optimization Algorithms with and for Large Language Models",
    "Main Contributions": "The paper introduces an LLM-driven objective discovery pipeline to automatically generate novel state-of-the-art preference optimization algorithms for Large Language Models (LLMs) without expert human intervention. The key finding is the discovery of Discovered Preference Optimization (DiscoPOP), also known as Log Ratio Modulated Loss (LRML), which is an adaptive blend of logistic and exponential losses. DiscoPOP demonstrates state-of-the-art performance and successful transfer to various held-out tasks, and it features surprising properties like a non-convex segment and negative gradients at the starting point.",
    "Methodology": "The core methodology is an iterative LLM-driven objective discovery pipeline. An LLM (GPT-4) is iteratively prompted to propose and implement new preference optimization loss functions in PyTorch code. These proposed functions are evaluated based on their performance metrics (e.g., MT-Bench scores) on a downstream validation task. This performance feedback is then provided back to the LLM as in-context examples for iterative refinement. The process aims to perform meta-optimization, searching for objective functions that maximize expected downstream performance. DiscoPOP itself is defined as a dynamically weighted sum of the logistic loss and exponential loss, where the weights are determined by a sigmoid calculation of the log-ratio difference with a temperature parameter.",
    "Experimental Setup": "For the discovery task, models were trained on multi-turn dialogue using the MT-Bench benchmark. The base LLM was 'zephyr-7b-gemma-sft', a 7 billion Gemma model fine-tuned on the 'deita-10k-v0-sft' dataset. Preference optimization was performed on the 'Argilla DPO Mix 7K' pairwise preference dataset. Training hyperparameters included a fixed beta=0.05, learning rate of 5e-7, bfloat16 format, two epochs, a batch size of two per device, gradient accumulation of eight, a cosine learning rate scheduler, and AdamW optimization. Models were trained using the TRL transformers library on 8 Nvidia A100 GPUs. Held-out evaluations included: 1) Single-turn dialogue on Alpaca Eval 2.0 (win rate against GPT-4 or the SFT base model). 2) Summarization on a subset (10%) of the Reddit TL;DR summarization preference dataset, evaluated using Alpaca Eval 2.0 with a custom dataset and GPT-4 annotator. 3) Positive sentiment generation for movie reviews on the IMDb dataset, using a GPT-2 model fine-tuned on IMDb and evaluated with a pre-trained sentiment classifier for rewards and KL-Divergence across a sweep of beta values.",
    "Limitations": "The current approach has several limitations. The method for generating LLM objective proposals is still nascent, with initial exploratory experiments using techniques like temperature sampling or context sorting showing no significant improvements. DiscoPOP, the discovered algorithm, struggles to converge when the beta parameter is too low (<=0.01) or too high (>=2.5), likely because the discovery process was fixed at beta=0.05. The non-convex nature of DiscoPOP can lead to training instability or model collapse, potentially requiring gradient clipping. The use of closed-source models (GPT-4) for code generation limits reproducibility and is costly. Furthermore, the pipeline's potential for misuse to generate unethical or harmful outputs is a concern, and LLM outputs are susceptible to hallucinations, necessitating content filters. The work also takes a small step towards code-level self-improvement, which could result in unintended behaviors.",
    "Future Research Directions": "Future research could focus on improving LLM objective proposal generation, for example, by leveraging more comprehensive training run information (e.g., learning curve plots provided to Visual Language Models) or by meta-optimizing LLM prompt templates. It would be beneficial to study different forms of the discovered objective functions, potentially incorporating multiple floating-point parameters that can be tuned separately, especially to address the stability issues observed with varying beta values in DiscoPOP. Finally, future work could explore using the models themselves to generate code (code-level self-improvement), moving away from reliance on costly, closed-source LLMs like GPT-4 to improve reproducibility and efficiency.",
    "Experiment Code": "def logistic_log_loss(\n    self,\n    policy_chosen_logps: torch.FloatTensor,\n    policy_rejected_logps: torch.FloatTensor,\n    reference_chosen_logps: torch.FloatTensor,\n    reference_rejected_logps: torch.FloatTensor,\n) -> torch.FloatTensor:\n    pi_logratios = policy_chosen_logps - policy_rejected_logps\n    ref_logratios = reference_chosen_logps - reference_rejected_logps\n    logits = pi_logratios - ref_logratios\n    losses = -F.logsigmoid(self.beta * logits)\n    return losses\n    ",
    "Experiment Result": "The 'logistic_log_loss' function is a direct implementation of the DPO (Direct Preference Optimization) loss. It calculates the difference between the policy's log-probabilities and the reference model's log-probabilities for chosen and rejected pairs. The loss is derived from the negative log-sigmoid of the scaled logits, aiming to increase the probability of preferred responses and decrease the probability of rejected responses."
}{
    "Title": "Language Generation with Strictly Proper Scoring Rules",
    "Main Contributions": "This paper proposes a straightforward strategy to adapt non-local strictly proper scoring rules, such as the Brier score and Spherical score, for training language generation models, addressing the limitation that only the local logarithmic score was previously feasible. It demonstrates that simply substituting the loss function with these alternative scores can yield substantial improvements in model generation capabilities, even without adjusting other hyperparameters. These improvements are shown to scale up to Large Language Models (LLMs) such as LLaMA-7B and LLaMA-13B. The work also introduces 'score smoothing,' a general technique to support label smoothing for arbitrary scoring rules.",
    "Methodology": "The core methodology involves adapting non-local scoring rules to sequence prediction by distributing the scoring rule at the token level. This is achieved by decomposing the sequence probability into a product of conditional token probabilities, effectively reducing the exponentially large sample space to the vocabulary size at each time step. The proposed loss function for token-level scoring is LS(θ) = −Ex∼q[ ∑t S(pθ(·|x<t), xt)]. For regularization, a general technique called 'score smoothing' is introduced: Sϵ(p, i) = (1− ϵ) · S(p, i) + ϵ/m · ∑j S(p, j). To enhance the smoothing effect for bounded scores like Brier and Spherical, a 'masked logarithmic score' is proposed, which applies the logarithmic score to 'under-smooth' labels (those with probabilities below ϵ/m).",
    "Experimental Setup": "Experiments were conducted on machine translation (WMT14 English-French, WMT14 English-German, TED bilingual dataset) and abstractive summarization (CNN/DailyMail). The model architectures used include Transformer-base and Transformer-big. For large language models, LLaMA-7B and LLaMA-13B were utilized as foundation models, fine-tuned via instruction tuning on the Alpaca dataset (52K instruction-following demonstrations). Evaluation metrics included BLEU for machine translation, ROUGE-1, ROUGE-2, and ROUGE-L for summarization, and MT-bench for assessing open-ended question answering capabilities of LLMs. Training involved both training from scratch and fine-tuning pre-trained models, with tests on generalized α-power and pseudo-spherical scores with varying α parameters.",
    "Limitations": "When training language generation models from scratch, alternative strictly proper scoring rules (Brier, Spherical) exhibited slower convergence and inferior performance compared to the logarithmic score. This is hypothesized to be due to their distinct learning dynamics (e.g., gradient characteristics) and hyperparameters that were initially optimized for the logarithmic score. A limitation of the general score smoothing technique for bounded scoring rules is that the corresponding loss can be relatively flat around the optimal smoothed distribution, leading to a weaker incentive for the model to genuinely perform label smoothing.",
    "Future Research Directions": "Future research could explore other strictly proper scoring rules that might exhibit superior performance during either pre-training or fine-tuning. Further investigation is needed into the underlying factors that contribute to the performance differences among various scoring rules, potentially leading to the development of new metrics or properties to determine a score's suitability for training language generation models. Additionally, the paper suggests exploring whether these scores could function as effective evaluation metrics for assessing the calibration of language generation models, similar to how perplexity is used.",
    "Experiment Code": null,
    "Experiment Result": null
}{
    "Title": "Geometric-Averaged Preference Optimization for Soft Preference Labels",
    "Main Contributions": "The research introduces distributional soft preference labels to address the binary preference assumption in LLM alignment, which often ignores subtle relationships and amplifies bias. The main contribution is a simple yet effective modification to Direct Preference Optimization (DPO) and its variants (IPO, ROPO) by incorporating a weighted geometric average of LLM output likelihood into the loss function. This approach adjusts the scale of learning loss, minimizing it when responses are equally preferred, and is shown to mitigate over-optimization and objective mismatch that prior works suffer from. It leads to models aligning with more preferable responses than those trained with binary labels.",
    "Methodology": "The core methodology involves modifying DPO-based loss functions by introducing 'soft preference labels' (ˆp ∈ [0.5, 1.0]) that estimate the true preference probability of one response over another. These soft labels are incorporated using a weighted geometric average of the LLM output likelihood. Specifically, for winner (yw) and loser (yl) outputs, the likelihood terms are re-weighted as π(yw|x)^ˆp * π(yl|x)^(1-ˆp) and π(yw|x)^(1-ˆp) * π(yl|x)^ˆp respectively. This leads to new algorithms: Geometric DPO (GDPO), Geometric IPO (GIPO), and Geometric ROPO (GROPO). The key effect is that the gradient scaling factor (wθ) for these methods includes a (2ˆp - 1) term, which forces the norm of gradients from equally preferable responses (ˆp ≈ 0.5) towards zero, thus reducing unnecessary model updates. It is also found that a larger β (regularization hyperparameter) can act as an implicit filter for soft preference labels, maintaining gradient scale for reliable pairs.",
    "Experimental Setup": "Experiments were conducted using PaLM 2-XS as the base LLM, with additional validation on Gemma-2B/7B. Soft preference labels were simulated using AI feedback from an instruction-tuned PaLM 2-L model, which calculates preference probability (ˆpAI) using the Bradley-Terry model and averaging over flipped orderings to mitigate position bias. Datasets include standard RLHF benchmarks like Reddit TL;DR, Anthropic Helpful, and Anthropic Harmless. To simulate diverse soft preference distributions, novel datasets based on Plasma Plan (natural language planning) were created, including 'Plasma Plan Skewed' and 'Plasma Plan Stairs' which feature a majority of modestly-confident labels. Evaluation involved pairwise comparison of responses from trained models (yllm) against reference responses from PaLM 2-L and GPT-4, using binary and percentage winning rates. Preference label classification accuracy was also measured on out-of-distribution pairs (e.g., PaLM 2-L vs. GPT-4/GPT-3.5). Online alignment performance was assessed using an extra reward model or self-preference feedback.",
    "Limitations": "The primary limitation is that experiments relied on AI feedback from LLMs to simulate soft preference labels due to cost constraints; leveraging actual human preferences collected via majority voting is left for future work. While AI ratings correlate well with human ratings (~80-85% agreement), the remaining disagreement could lead to accumulation of errors in iterative feedback processes, necessitating continuous human monitoring to ensure safety. The study also acknowledges the potential for synthetic data to cause catastrophic performance degradation due to data distribution shifts, emphasizing the need to be mindful of this during post-training alignment. Furthermore, the paper implicitly notes that while some datasets are dominated by high-confidence labels, it's important to rethink the effect of richly distributed soft preference data on performance.",
    "Future Research Directions": "Future research directions include leveraging actual human preferences labeled via majority voting, rather than simulated AI feedback, to validate the approach. Another important area is rethinking the effect of preference data distribution on model performance, especially exploring the benefits of richly distributed soft preference labels compared to high-confidence concentrated ones. Further investigation into the trade-off between conflicting preferences (e.g., helpfulness vs. harmlessness) and how algorithms can handle such multi-attribute preferences is also suggested. Additionally, optimizing the number of gradient steps for generated sample reuse and the number of responses sampled per prompt in online alignment experiments remains an interesting direction. Detecting label bias and debiasing preference labels are also highlighted as crucial for future study to ensure LLM outputs align with social agreements and common sense.",
    "Experiment Code": null,
    "Experiment Result": null
}{
    "Title": "Adaptive Budget Allocation for Parameter-Efficient Fine-Tuning ",
    "Main Contributions": "The paper addresses the suboptimal performance of existing parameter-efficient fine-tuning methods (like LoRA) that evenly distribute the budget of incremental updates across all pre-trained weight matrices, overlooking their varying importance. AdaLoRA is proposed to adaptively allocate the parameter budget among weight matrices based on their importance score. It parameterizes incremental updates using singular value decomposition (SVD) and effectively prunes singular values of unimportant updates to reduce parameter budget, circumventing intensive exact SVD computations. This approach significantly improves fine-tuning performance, especially in low budget settings, achieving notable gains over baselines.",
    "Methodology": "AdaLoRA's methodology consists of two core components: (i) SVD-based adaptation: Incremental updates (∆) to pre-trained weight matrices are parameterized as ∆ = PΛQ, where Λ is a diagonal matrix of singular values and P, Q are orthogonal matrices representing singular vectors. An orthogonality regularizer is added to the training loss to enforce P⊤P = QQ⊤ = I, avoiding intensive exact SVD computations. (ii) Importance-aware rank allocation: The rank of ∆ is dynamically adjusted by pruning singular values based on an importance score. A novel importance metric is designed that combines the singular value and vectors, using a smoothed sensitivity and uncertainty quantification (I(t) * U(t)) for each entry. Singular values of triplets with low importance scores are zeroed out. A global budget scheduler is also introduced, starting with a slightly higher initial budget and gradually reducing it to the target using a cubic schedule, followed by a fixed fine-tuning phase.",
    "Experimental Setup": "Experiments were conducted on various tasks and models. For Natural Language Understanding (NLU) and Question Answering (QA), DeBERTaV3-base was fine-tuned on GLUE benchmark (MNLI, SST-2, CoLA, QQP, QNLI, RTE, MRPC, STS-B) and SQuAD v1.1/v2.0 datasets. For Natural Language Generation (NLG), BART-large was fine-tuned on XSum and CNN/DailyMail datasets. AdaLoRA was compared against Full fine-tuning, Bitfit, Houlsby Adapter, Pfeiffer Adapter, and a generalized LoRA (applied to all weight matrices). Evaluation metrics included accuracy, Matthews correlation coefficient (Mcc), F1 score, Exact Match (EM), and ROUGE 1/2/L (R-1/2/L). Experiments were run under different parameter budget levels (e.08% to 12.5% of total pre-trained parameters) and involved tuning hyperparameters like learning rate, regularization coefficient, and budget scheduler parameters.",
    "Limitations": "The paper does not explicitly state limitations of AdaLoRA itself, but rather highlights how AdaLoRA overcomes limitations of previous methods like diff pruning (reliance on low-level implementation for unstructured sparse matrices, high computational cost) and LoRA (prespecified identical rank, suboptimal performance due to even budget distribution). Implicitly, AdaLoRA does incur some additional training overhead compared to LoRA, with an observed 11-16% increase in training time and slightly higher memory footprint, as shown in the training cost comparison. The orthogonality regularization also adds complexity to the training objective.",
    "Future Research Directions": "Not mentioned",
    "Experiment Code": "examples/NLG/eval/eval.py",
    "Experiment Result": "-R REFERENCE -H HYPOTHESIS -nr 4 -m bleu,meteor,chrf++,ter,bert,bleurt"
}
- Pay attention to how each study builds upon previous work and which limitations remain unresolved. Organize this information to form a clear picture of the current research landscape.
- Identify significant gaps, challenges, or unmet needs that persist across these studies. Consider whether there are opportunities to apply methods or concepts from other domains to overcome these limitations.
- Reflect on aspects that have not yet been explored or could be improved (e.g., new techniques, new evaluation metrics, novel datasets, or approaches to generalizing findings). Ensure that your idea is broadly applicable and not overly dependent on a specific dataset or model.
- When selecting existing studies as the basis for the new method, please prioritize those that have Python implementations available.
- Please limit research ideas to those that can be validated with a Python script.
- Output content:
    Based on the above analysis, propose a new research idea that meaningfully advances the field. Your output should include:
    - open_problems
        - Select one key challenge in the target research area and provide a detailed summary of it.
        - Choose a problem whose resolution would have a significant impact.
    - methods
        - Provide a detailed description of the approach for solving the identified problem.
        - Describe a high-level strategy, specifying what new components, features, or modifications should be added to existing systems or codebases, what data or algorithms are needed, and how this differs from prior work.
    - experimental_setup
        - Provide a concrete description of the experiments to demonstrate the effectiveness of the proposed method.
        - Specify which models and datasets will be used, the evaluation metrics for quantitative assessment, and how comparisons will be made with baseline methods.
        - Design experiments to evaluate robustness from multiple perspectives.
    - experimental_code
        - Output the core Python code of the new research idea to be used in the experiment.
        - Create it based on the experimental code of the study used as the foundation.
    - expected_result
        - Describe the expected experimental results based on the "experimental_setup."
    - expected_conclusion
        - Summarize the academic and practical value of the new method based on the experimental results.
        - Discuss its potential for future development.
Output:
{
    "open_problems": "Current preference-based fine-tuning losses (DPO, DiscoPOP, GDPO) suffer from three coupled weaknesses: (1) Gradient/optimization instability – all widely-used objectives are un-bounded below, so large log-ratio errors explode the gradient and training can collapse or require per-loss tuning of β, gradient-clipping, etc.; (2) Poor calibration – the probability σ(βΔ) a model implicitly assigns to the chosen answer is badly calibrated, harming iterative RLHF pipelines that rely on these probabilities as synthetic labels; (3) Limited ability to exploit label uncertainty – binary or soft-label variants rescale the same cross-entropy style loss but do not change its shape, so noisy labels still receive large gradients.  A loss that is simultaneously bounded, strictly proper (so calibrated), and naturally down-weights uncertain labels would address all three issues and make preference optimisation more robust across models, domains and β settings.",
    "methods": "We propose Bounded Proper-Scoring Preference Optimisation (BPPO).  Key ideas:\n1. Strictly proper scoring rule: replace the unbounded log-loss used in DPO with the Brier score (or Spherical score).  For a preference probability p̂ in [0,1] and true soft label y∈[0,1] the token loss is S_brier(p̂,y)=(p̂−y)^2, which is bounded in [0,1].  This guarantees calibrated probabilities and finite gradients.\n2. Preference probability head: As in DPO we compute Δ = (π_chosen−π_rejected) − (π_ref_chosen−π_ref_rejected).  We map Δ→p̂ = σ(βΔ).  β is still a temperature but the loss is now bounded and much less sensitive to β.\n3. Uncertainty-aware re-weighting: When the soft label y is near 0.5 the squared error term is naturally small, but we further multiply each example by w = |y−0.5|^γ (γ≈1).  Highly uncertain pairs therefore contribute little gradient without ad-hoc thresholding.\n4. Mixed scoring (optional): To recover the sharpness benefits of CE we add a small cross-entropy term: L = λ·Brier + (1−λ)·Logistic.  λ∈[0.7,1].\n5. Minimal code change: BPPO is a drop-in replacement for the current DPO loss – only ~15 lines of PyTorch.\nHow it differs from prior work: (a) First use of bounded, strictly proper scoring rules for pair-wise LLM preference optimisation; (b) Unified treatment of calibration and uncertainty via the same loss instead of external label smoothing or geometric averaging; (c) Eliminates the exploding-gradient issue noted for DiscoPOP and the slow convergence of proper-scoring rules in language modelling by restricting the loss to the 1-D preference probability rather than the full vocabulary.",
    "experimental_setup": "Models: 1) zephyr-7b-gemma-sft (same as DiscoPOP); 2) LLaMA-2-7B-chat for cross-model validation.\nDatasets: a) Argilla DPO Mix 7K (binary); b) Reddit TL;DR + Anthropic Harmless with GPT-4 soft labels (ˆp) to test uncertainty handling.\nBaselines: DPO, DiscoPOP (LRML), GDPO.  All use identical hyper-parameters except the loss.\nMetrics:\n • MT-Bench win-rate (against GPT-4) – alignment quality.\n • AlpacaEval 2.0 single-turn win-rate.\n • Summarisation: ROUGE-L on Reddit TL;DR held-out.\n • Calibration: Expected Calibration Error (ECE) over σ(βΔ) vs empirical win-rate on a 5K held-out preference set.\n • Stability: fraction of runs (5 seeds) that diverge; gradient norm statistics.\nProtocol: Two epochs, lr 5e-7, β∈{0.02,0.05,0.5,1,2} to test sensitivity.  Evaluate after every 500 steps.  Same hardware (8×A100).",
    "experimental_code": "import torch, torch.nn.functional as F\n\ndef bppo_loss(policy_chosen_logps: torch.Tensor,\n              policy_rejected_logps: torch.Tensor,\n              reference_chosen_logps: torch.Tensor,\n              reference_rejected_logps: torch.Tensor,\n              soft_labels: torch.Tensor = None,\n              beta: float = 0.05,\n              gamma: float = 1.0,\n              lam: float = 0.8):\n    \"\"\"Bounded Proper-Scoring Preference Optimisation (BPPO).\n    soft_labels  y in [0,1]; if None assume y=1 (binary) for chosen>rejected.\n    Returns per-example loss tensor. \"\"\"\n    # log-ratio difference between policy and reference\n    pi_lr   = policy_chosen_logps - policy_rejected_logps\n    ref_lr  = reference_chosen_logps - reference_rejected_logps\n    delta   = pi_lr - ref_lr\n    p_hat   = torch.sigmoid(beta * delta)              # model preference prob\n    if soft_labels is None:\n        y = torch.ones_like(p_hat)                      # binary winner label\n    else:\n        y = soft_labels.clamp(0.,1.)\n    # Brier score (bounded) & optional logistic CE\n    brier = (p_hat - y) ** 2\n    ce    = F.binary_cross_entropy(p_hat, y, reduction='none')\n    # uncertainty weight |y-0.5|^gamma\n    w = (y - 0.5).abs() ** gamma * 2  # scale to [0,1]\n    loss = w * (lam * brier + (1 - lam) * ce)\n    return loss",
    "expected_result": "1. Quality: BPPO matches or exceeds DiscoPOP on MT-Bench (e.g., 71 → 72) and AlpacaEval, and significantly outperforms DPO on soft-label datasets (+3-5% win rate).\n2. Calibration: ECE drops from ~8% (DPO) and 11% (DiscoPOP) to <3% with BPPO.\n3. Stability: Zero collapse events over all β; gradient norms stay within 2× of median without clipping, while DiscoPOP collapses for β≤0.01 or ≥2.5.\n4. β-robustness: Performance curve of BPPO is flat across β in [0.02,2]; DPO and DiscoPOP degrade >5 points outside their tuned β.",
    "expected_conclusion": "BPPO shows that bringing bounded strictly proper scoring rules into preference optimisation yields more stable training, better calibrated probability estimates, and competitive or superior alignment quality—all with a tiny code change.  The method removes much of the β hyper-parameter sensitivity and obviates aggressive gradient-clipping, simplifying large-scale alignment pipelines.\nFuture work: explore other bounded scores (e.g., neighbourhood-aware Continuous Ranked Probability Score), investigate online human-in-the-loop preference collection that directly uses the calibrated p̂, and combine BPPO with adaptive loss discovery pipelines to automatically select the optimal proper scoring rule for a given domain."
}
